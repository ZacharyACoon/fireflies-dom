import React from 'react';
import ReactDOM from 'react-dom';
import FireflySync from "./firefly-sync/firefly_sync"


ReactDOM.render(
  <React.StrictMode>
    <FireflySync/>
  </React.StrictMode>,
  document.getElementById('root')
);
